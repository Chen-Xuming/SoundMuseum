<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.soundmuseum">
    <!-- 百度地图 -->
    <!-- 访问网络，进行地图相关业务数据请求，包括地图数据，路线规划，POI检索等 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 获取网络状态，根据网络状态切换进行数据请求网络转换 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 读取外置存储。如果开发者使用了so动态加载功能并且把so文件放在了外置存储区域，则需要申请该权限，否则不需要 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 写外置存储。如果开发者使用了离线地图，并且数据写在外置存储区域，则需要申请该权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".formatConvert.FormatConvertActivity"></activity>
        <activity android:name=".dbMeter.DBMeterActivity" />

        <activity android:name=".fm.AudioFmActivity" />
        <activity android:name=".TestKotlinActivity" />
        <activity android:name=".map.MapActivity" /> <!-- 百度地图API -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="BrafixOya1Z4M1ZP3ZIcOnMgWL9UZwTt" /> <!-- 音频裁剪工具 -->
        <activity
            android:name="com.ringtone.maker.Activities.Activity_Editor"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait">
            <intent-filter android:label="Edit">
                <action android:name="android.intent.action.EDIT" />

                <data android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.OPENABLE" />

                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".account.AccountActivity"
            android:theme="@style/SeachViewActivityTheme"/>
        <activity
            android:name=".record.RecordActivity"
            android:theme="@style/SeachViewActivityTheme"/>
        <activity android:name=".UserCenterActivity" />
        <activity
            android:name=".search.MySearchActivity"
            android:theme="@style/SeachViewActivityTheme" />
        <activity android:name=".MyCenterActivity" />
        <activity android:name=".SendActivity"
            android:theme="@style/CreateActivityTheme"/>
        <activity android:name=".register" />
        <activity android:name=".login" />


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>